
    <div class="header-pack">
      <a href="/products">Shop</a>
      <a href="">Energy</a>
      <a href="">About us</a>
      <a href="">Rewards</a>
    </div>

    <% if(!locals.isAuth) {%>
    <nav id="user-img"><a href="/login"> <img src="\images\user-regular.svg" alt="user" />  </a>
    
      <div>
        <% if(!locals.isAdmin) {%>
       <a href="/cart">
        <button aria-label="Cart" id="solar-cart-btn">
          <div>
            Cart
          </div> 
            <!-- <img src="\images\cart-shopping-solid.svg" alt="cart"> -->
            <span class="badge"><%= locals.cart.totalQuantity %></span>
        </button>
       </a>
        <% } %>
      </div>
   <% } %>
    </nav>
    <% if(locals.isAuth ) {%>
    <nav id="user-img" class="logged-userImg"><a href="/loggedIn-profile"> <img src="\images\user-regular.svg" alt="user" />  </a>
    
      <div>
        <% if(!locals.isAdmin) {%>
          <a href="/cart">
            <button aria-label="Cart" id="solar-cart-btn">
              <div>
                Cart
              </div> 
                <!-- <img src="\images\cart-shopping-solid.svg" alt="cart"> -->
                <span class="badge"><%= locals.cart.totalQuantity %></span>
            </button>
          </a>
       
        <% } %>
      </div>
      <% } %>
      </nav>
 
      <% if (locals.isAdmin) { %>
       <li><a href="/admin/products"> Manage Products</a></li>
       <li><a href="/admin/orders"> Manage Orders</a></li>
    
      <% } %>
 
 